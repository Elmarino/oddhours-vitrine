'use client';

import { Card, CardHeader, CardTitle } from '@/src/components/ui/card';
import React from 'react';

const Error = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>⚠︎⚠︎⚠︎😀 Error 🥰⚠︎⚠︎⚠︎</CardTitle>
      </CardHeader>
    </Card>
  );
};

export default Error;
